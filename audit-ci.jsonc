{
  // $schema provides code completion hints to IDEs.
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  // Match previous `npm audit --audit-level=low` behavior.
  "low": true,
  "allowlist": [
    // image-size via web-ext → addons-linter: no patched release yet (all ≤2.0.2 flagged).
    // Remove when image-size >2.0.2 ships or addons-linter drops/replaces it.
    {
      "GHSA-w3rx-r6r6-pgpr": {
        "active": true,
        "notes": "https://github.com/advisories/GHSA-w3rx-r6r6-pgpr — ICNS DoS in image-size (via web-ext)",
        "expiry": "2026-12-11"
      }
    },
    {
      "GHSA-5p2g-fcmc-qvqq": {
        "active": true,
        "notes": "https://github.com/advisories/GHSA-5p2g-fcmc-qvqq — JXL/HEIF DoS in image-size (via web-ext)",
        "expiry": "2026-12-11"
      }
    },
    // adm-zip via web-ext → firefox-profile: latest adm-zip (0.6.0) still flagged.
    // Remove when adm-zip >0.6.0 ships a fix or firefox-profile/web-ext stops depending on it.
    {
      "GHSA-vwc7-r8mq-g2x9": {
        "active": true,
        "notes": "https://github.com/advisories/GHSA-vwc7-r8mq-g2x9 — adm-zip symlink overwrite (via web-ext)",
        "expiry": "2026-12-11"
      }
    }
  ]
}
