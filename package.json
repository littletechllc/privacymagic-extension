{
  "name": "privacymagic",
  "version": "0.0.1",
  "description": "a browser extension that protects your privacy online",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --format eslint-formatter-unix && tsc --noEmit",
    "format": "eslint . --fix --format eslint-formatter-unix && tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy": "node tools/copy-src.ts src dist",
    "copy:watch": "npm run copy -- --watch",
    "build": "npx tsx tools/adblock-list-processor.ts && npm run copy && NODE_ENV=production node esbuild.config.js && web-ext build --source-dir dist --overwrite-dest --artifacts-dir artifacts",
    "dev": "concurrently \"npm run copy:watch\" \"NODE_ENV=development node esbuild.config.js watch\" \"web-ext run -t chromium --source-dir dist --ignore-files _metadata _metadata/**\""
  },
  "author": "Arthur Edelstein",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "dependencies": {
    "psl": "1.15.0",
    "punycode-npm": "npm:punycode@2.3.1"
  },
  "devDependencies": {
    "@types/chrome": "0.1.31",
    "chokidar": "5.0.0",
    "concurrently": "9.2.1",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "eslint-formatter-unix": "^9.0.1",
    "globals": "^15.0.0",
    "tsx": "^4.21.0",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.52.0",
    "web-ext": "9.2.0"
  },
  "engines": {
    "node": "22.20.0"
  }
}
